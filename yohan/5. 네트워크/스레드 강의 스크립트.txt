스레드
-메인스레드

*기본적으로 메인스레드에서 접근하고 관리한다

*멀티스레드에서도 메인스레드외에  스레드에서 메모리 접근이 불가능하다.
*  데드락현상이라고한다. 
*스레드는 동일 프로세스 내에 있으므로 작업수행의결과를 동시처리가능
*스레드 UI객체 직접 접근 불가능, 핸들러 객체 사용

*스레드 만드는 방법 두가지:  스레드 상속, 스레드 클래스를 만들어서 정의
*핸들러: 메시지 큐를 이용해 메인 스레드에서 처리할 메시지를 전달하는 역할, 특정 메시지가 미래어떤 시점에 실행되도록 스케쥴링할 수 있음

*핸들러를 사용할 때 필요한 세가지 단계
 obtain Message(): 메시지 객체를 리턴받음 -> sendMessage() 메시지 큐에 넣음->handleMessage()메소드에 정의된 기능 수행, 메인스레드에서 수행됨.

? try catch가 모에요?

*스레드에서 run 호출되고 start호출된다.

value가 바뀌는건 ui에 접근해서 하는게 아니고 스래드 내부에서 값만 바꿔주는 것이다.


메인스레드에선 UI에 접근하기 때문에 문제 발생하지 않는다.

스레드에서 직접 UI접근이 안되므로 핸들러 객체를 만들어서 핸들러에게 sendmessage를 하여 핸들러가
그 메시지를 받으면 핸들러는 메인스레드 내에서 작동하므로 UI객체 접근가능하므로 메시지내용을수행할수있다

? Message message = handler.obtainMessage(); 어떤 값을 넣겠다는건가?

AsyncTask 클래스를 정의하고 
쓰레드로 동작하는 코드 , UI로 접근하는 코드를
메서드로만 분리를 하면



